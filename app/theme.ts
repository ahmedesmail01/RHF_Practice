"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  // Customize your theme here
  palette: {
    primary: {
      main: "#1976d2",
    },
    secondary: {
      main: "#dc004e",
    },
  },
  // You can add more customizations like typography, spacing, etc.
});

export default theme;
